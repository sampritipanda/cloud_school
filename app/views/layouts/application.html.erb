<!DOCTYPE html>
<html>
<head>
  <title>Cloud School</title>
  <!--[if lt IE 9]>
  	<script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="texxt/javascript""></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  
  <style>
  	body {
		<% if (params[:controller] == "sites" && (params[:action] == "join" || params[:action] == "new")) || (/^(devise)/.match(params[:controller])) %>
			background-color:#A0AA6D;
			background-image:url('/images/notebook_paper2.jpg');
			background-repeat:no-repeat;
			background-position:50% -3%;
			background-attachment:scroll;
		<% else %>
			background-color:#A0AA6D;
		<% end %>
	}
  </style>
    
</head>
<body>

<div class="container-fluid">
	
	<!-- Temp Start ->
	<div class="row">
		<div class="span3">
			<h1><img src="http://placehold.it/400x100&text=Logo" /></h1>
		</div>
		<div class="span9">
			<% if user_signed_in? %>
				Signed in as <%= current_user.username %>. Not You?
				<%= link_to "Sign Out", destroy_user_session_path, :method => :delete%> 
			<% end %>
		</div>
	</div>
	<!-- Temp Start -->
	
	<% if user_signed_in? %>
		Signed in as <%= current_user.username %>. Not You?
		<%= link_to "Sign Out", destroy_user_session_path, :method => :delete%> 
	<% end %>
	
	<% flash.each do |name, msg| %>
	  <%= content_tag :div, msg, :class => "flash #{name}" %>
	<% end %>

	<!--<p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>-->
	
	<%= yield %>
</div>

</body>
</html>
