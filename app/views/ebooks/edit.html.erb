<div id="ebook-form"><center>
	<%= render :partial => 'form', :locals => {:url => "site_ebook_path(current_user.site.id, @ebook.id)", :type => "Edit"} %>
	
	<%= link_to 'Show', site_ebook_path(current_user.site.id, @ebook.id) %> |
	<%= link_to 'Back', site_ebooks_path(current_user.site.id) %>
</center></div>